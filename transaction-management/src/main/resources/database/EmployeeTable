CREATE DATABASE employee;

USE employee;

CREATE TABLE Employee (
    id VARCHAR(20) NOT NULL,
    gender TINYTEXT,
    city TINYTEXT,
    area TINYTEXT,
    state TINYTEXT,
    pin TINYTEXT,
    PRIMARY KEY (id)
);

select * from Employee;

truncate Employee;